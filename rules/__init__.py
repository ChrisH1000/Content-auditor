"""Rules package

Contains zero-cost, deterministic rule checks for SEO and accessibility
that can be run offline (no LLM required). These functions return JSON
serializable dicts suitable for aggregation into reports.
"""

