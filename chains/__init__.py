"""Chains package

This package contains high-level chains that orchestrate LLM calls and
provide structured outputs used by the CLI. Each module in `chains/` is
kept intentionally small and focused (for example `tone_chain.py`).
"""
